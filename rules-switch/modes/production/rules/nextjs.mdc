---
description: PRODUCTION Next.js—strict server/client split; centralized Yahoo typed helper; no inline duplication; structured API errors (zh-TW).
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# Next.js（PRODUCTION）

## Server／Client

- 預設 Server Component；**only** `'use client'` 當需要 hooks／瀏覽器／訂閱輪詢。
- **`NEXT_PUBLIC_*`** 僅公開值。**禁止** service role、`TELEGRAM_*` 出現在 client bundle。

## 架構與檔案

- **Yahoo**：**單一模組** exported typed `fetchNormalizedQuote(symbol)`（或同等）；含 timeout、錯誤對應 UI。
- Route Handlers：`NextResponse.json`／一致 HTTP semantics；錯誤 body **不包含** secrets。
- **禁止**在多個元件複製 fetch／parse；資料取得可抽 **`lib/`** hooks 或由 server fetch + props。

## UI

- Tailwind + 基本 a11y（語意標籤、`aria-*` 若適用）。
- Recharts：獨立 client 子元件 + typed props。
- 輪詢：cleanup；間隔對照 spec（約 60s）；考慮 **退避**或 **AbortController**。

## Cron（選做）

- 若使用 Vercel Cron：**驗證** `CRON_SECRET` 或同等；行為寫入 spec／註解。

## Do / Don't

| Do | Don't |
| --- | --- |
| 機密與特權只走 server | client 呼叫 Telegram／service role |
| 快取策略明確（即時股價 no-store） | 默認快取導致舊價 |

## 參照

- [`docs/spec.md`](docs/spec.md)、[`AGENTS.md`](AGENTS.md)
