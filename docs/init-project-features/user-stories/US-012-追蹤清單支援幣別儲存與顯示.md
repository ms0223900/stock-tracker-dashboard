### US-012：追蹤清單支援幣別儲存與顯示

**作為** 使用者
**我想要** 儲存追蹤項目時一併記錄幣別，重新整理頁面後仍能看到正確的幣別資訊
**以便** 我追蹤不同市場的股票時不會混淆計價貨幣

**輸入格式**：
- `StockPrice.currency`（由 US-010 解析，例如 `"TWD"`、`"USD"`）
- 現有 `watchlist` 資料表結構與 `WatchlistItem` 型別

**輸出格式**：

**資料庫**：
- 新增 migration `002_add_currency.sql`，在 `watchlist` 資料表新增 `currency` 欄位：
  - `currency TEXT NOT NULL DEFAULT 'TWD'`
- 寫入 migration 檔案 `supabase/migrations/002_add_currency.sql`

**型別**：
- `WatchlistItem` 型別新增 `currency: string` 欄位

**儲存邏輯**（`hooks/useWatchlist.ts`）：
- `handleSave()` 從 `stockData.currency` 讀取並寫入 `watchlist` 資料表的 `currency` 欄位
- 向下相容不影響既有的舊資料（預設 `'TWD'`）

**顯示邏輯**（`components/WatchlistCard.tsx`）：
- `formatPrice(item.last_price, item.currency)`
- `formatPrice(item.target_price, item.currency)`

**後端排程**（`app/api/check-prices/route.ts`）：
- `fetchStockPriceServer` 回傳的 `currency` 僅供記錄，不影響比價邏輯

**驗收條件**：
- [x] 執行 migration 後 `watchlist` 資料表有 `currency` 欄位
- [x] 儲存 `2330.TW` 時 DB 記錄 `currency = 'TWD'`
- [x] 儲存 `AAPL` 時 DB 記錄 `currency = 'USD'`
- [x] 重新整理頁面後 watchlist 仍顯示正確的幣別前綴
- [x] 既有資料（migration 前或缺欄位）以預設 `'TWD'` 顯示 `NT$` 前綴，不報錯
- [x] 追蹤清單可同時顯示多種幣別的股票（如 TWD + USD 混排）
- [x] 輪詢更新股價時，記憶體中的 `currency` 與 Yahoo 回傳一致（供即時顯示）

#### 驗收說明

**整體結論**：PASS ✅

> Migration `002_add_currency.sql` 新增 `currency` 欄位；`handleSave` 寫入 `stockData.currency`；`fetchWatchlist` 以 `?? "TWD"` 向下相容；輪詢時同步更新 `item.currency`。

---

**AC-1：[migration 新增 currency 欄位]**

狀態：✅ 通過

- `supabase/migrations/002_add_currency.sql`：`currency TEXT NOT NULL DEFAULT 'TWD'`。

---

**AC-2～AC-3：[儲存時寫入正確幣別]**

狀態：✅ 通過

- `hooks/useWatchlist.ts` 的 `insert` 含 `currency: stockData.currency`；幣別來源為 `lib/yahoo-finance.ts` 解析之 `meta.currency`。

---

**AC-4：[刷新後仍正確顯示]**

狀態：✅ 通過

- `select("*")` 帶回 `currency`；`WatchlistCard` 以 `formatPrice(..., item.currency)` 顯示。

---

**AC-5：[既有資料向下相容]**

狀態：✅ 通過

- DB `DEFAULT 'TWD'`；client `row.currency ?? "TWD"`，舊列不報錯。

---

**AC-6：[多幣別混排]**

狀態：✅ 通過

- 每筆 `watchlist` 各自帶 `currency`，`formatPrice` 依筆格式化。

---

**AC-7：[輪詢同步 currency]**

狀態：✅ 通過

- `hooks/useWatchlistPolling.ts` 輪詢時將 `d.currency` 寫回 state。

**依賴關係**：
- US-010（解析 Yahoo Finance 幣別資訊）
- US-005（既有 Supabase migration 基礎）
- US-008（既有追蹤清單 CRUD）

**優先級**：P1
**相關功能**：追蹤清單、資料庫 migration；**US-001 AC2** 於查詢結果之幣別顯示，本 US 延伸至追蹤清單持久化
