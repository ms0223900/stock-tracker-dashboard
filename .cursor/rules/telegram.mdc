---
description: PROTOTYPE Telegram—server-only; alert when price >= target and not notified; success then mark is_notified.
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# Telegram（PROTOTYPE）

## 絕對底線

- **僅 server** 呼叫 Bot API；環境變數見 [`docs/spec.md`](docs/spec.md) §10。
- 觸發：`currentPrice >= targetPrice` 且 `is_notified === false`。
- **先發送成功**，再 `is_notified = true`、`notified_at`。
- 發送失敗：**不**將 `is_notified` 設為 `true`。

## PROTOTYPE 可取捨

- 訊息格式可簡化，但仍須含 **代號、現價、目標價、時間**（對照 spec 範例為佳）。
- retry／排程可選；**不要**對 API 無節流無限重試。

## Do / Don't

| Do | Don't |
| --- | --- |
| 失敗時維持未通知 | 失敗仍標已通知 |
| 純文字訊息降低失敗率 | 未 escape 的複雜 parse_mode（非必要） |

## 參照

- [`docs/spec.md`](docs/spec.md)、[`AGENTS.md`](AGENTS.md)
